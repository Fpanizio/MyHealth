import {
  View,
  Text,
  StyleSheet,
  Dimensions,
  Image,
  TouchableOpacity,
} from 'react-native';

const Card = props => {
  const {item} = props.item;

  

  
};

const styles = StyleSheet.create({
  
});

export default Card;
